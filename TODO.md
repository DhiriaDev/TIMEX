- [x] Use environment variables to automatically set SSL/SASL only on the cluster - 2022-12-01
- [ ] Modify `validation` to return a JSON structure of the result, not an array of timexcontainer objects
